//Import Needed Components
import TransactionPin from "@/components/AuthComponents/TransactionPin";

const Page = () => {
   
  return (
    <main>
        <TransactionPin />
    </main>
  );
};

export default Page;
