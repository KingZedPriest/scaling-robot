//Import Needed Components
import VerificationForm from "@/components/AuthComponents/VerificationForm";

const page = () => {
  return (
    <main>
        <VerificationForm />
    </main>
  );
};

export default page;
